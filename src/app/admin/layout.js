export const metadata = {
  title: 'Admin Panel - Tamil Gaming',
  description: 'Administrative interface for managing Tamil Gaming YouTube data synchronization',
  robots: 'noindex, nofollow'
};

export default function AdminLayout({ children }) {
  return children;
}